{
  "name": "architech-site",
  "version": "1.0.0",
  "private": true,
  "description": "Architech marketing site — Express server serving the static front-end plus /api/lead (Postgres), /api/checkout and /api/webhook (Stripe subscriptions). Railway-ready.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "stripe": "^18.5.0"
  }
}
